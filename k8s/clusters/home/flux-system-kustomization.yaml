---
# This Kustomization resource tells Flux how to decrypt SOPS-encrypted secrets
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m0s
  path: ./k8s/clusters/home
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  # CRITICAL: This tells Flux to use Age decryption provider
  decryption:
    provider: age
    secretRef:
      name: sops-age
