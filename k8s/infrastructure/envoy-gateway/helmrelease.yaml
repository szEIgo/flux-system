apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: envoy-gateway
  namespace: envoy-gateway-system
spec:
  interval: 1h0m0s
  chart:
    spec:
      chart: envoy-gateway
      sourceRef:
        kind: HelmRepository
        name: envoy-gateway
        namespace: envoy-gateway-system
  values:
    deployOperator: true
    installCRDs: true
    gatewayAPI:
      enabled: true
