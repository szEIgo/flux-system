apiVersion: v1
kind: Namespace
metadata:
  name: teknik

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: teknik-www
  namespace: teknik
spec:
  storageClassName: zfs-fast
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: teknik
  namespace: teknik
spec:
  replicas: 1
  selector:
    matchLabels:
      app: teknik
  template:
    metadata:
      labels:
        app: teknik
    spec:
      initContainers:
      - name: content-sync
        image: alpine/git:latest
        env:
        - name: GITHUB_TOKEN
          valueFrom:
            secretKeyRef:
              name: teknik-github-token
              key: token
        command: ["/bin/sh","-c"]
        args:
          - |
            set -eu
            target=/data/www
            mkdir -p "$target"
            if [ -d "$target/.git" ]; then
              git -C "$target" fetch --all || true
              git -C "$target" reset --hard origin/main || true
            else
              git config --global advice.detachedHead false
              repo=https://$GITHUB_TOKEN@github.com/szEIgo/norsemen_teknik.git
              git clone --depth 1 "$repo" "$target"
            fi
        volumeMounts:
        - name: www
          mountPath: /data/www
      containers:
      - name: teknik
        image: nginx:alpine
        ports:
        - containerPort: 80
        volumeMounts:
        - name: www
          mountPath: /usr/share/nginx/html
      volumes:
      - name: www
        persistentVolumeClaim:
          claimName: teknik-www

---
apiVersion: v1
kind: Service
metadata:
  name: teknik
  namespace: teknik
spec:
  selector:
    app: teknik
  ports:
  - port: 80
    targetPort: 80

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: teknik
  namespace: teknik
  annotations:
    cert-manager.io/cluster-issuer: "selfsigned"
spec:
  ingressClassName: traefik
  tls:
  - hosts:
    - teknik.szigethy.lan
    secretName: teknik-tls
  rules:
  - host: teknik.szigethy.lan
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: teknik
            port:
              number: 80

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: teknik-public
  namespace: teknik
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: traefik
  tls:
  - hosts:
    - teknik.norsemen.io
    secretName: letsencrypt-tls
  rules:
  - host: teknik.norsemen.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: teknik
            port:
              number: 80