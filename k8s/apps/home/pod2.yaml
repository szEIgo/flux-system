---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pod2
  namespace: apps
  labels:
    app: pod2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pod2
  template:
    metadata:
      labels:
        app: pod2
    spec:
      containers:
      - name: pod2
        image: ghcr.io/traefik/whoami:latest
        ports:
        - name: http
          containerPort: 80
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: pod2
  namespace: apps
  labels:
    app: pod2
spec:
  selector:
    app: pod2
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  type: ClusterIP
---
# Public .dk Ingress using Let's Encrypt
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pod2-dk
  namespace: apps
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - pod2.szigethy.dk
    secretName: pod2-dk-tls
  rules:
  - host: pod2.szigethy.dk
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: pod2
            port:
              number: 80
---
# LAN .lan Ingress using self-signed
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pod2-lan
  namespace: apps
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - pod2.szigethy.lan
    secretName: szigethy-lan-tls
  rules:
  - host: pod2.szigethy.lan
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: pod2
            port:
              number: 80
